<% provide(:title, 'Accounts') %>
<% provide(:titleSmall, 'All') %>

<% provide :actions, link_to(raw('<i class="icon-plus icon-white"></i> New Account'), new_account_path, class: "btn btn-small btn-primary") %>

<%= render 'layouts/pagination' %>

<table class="table table-striped table-bordered table-hover">
  <tr>
    <th>Name</th>
    <th>Balance</th>
    <th></th>
  </tr>

  <%=render @accounts %>

</table>

<%= render 'layouts/pagination' %>

<% provide :leftNavButtonBody, 'Home' %>
<% provide :leftNavButtonUrl, root_path %>