<% provide(:title, 'All Tags') %>

<% provide :actions, link_to(raw('<i class="icon-plus icon-white"></i> New Tag'), new_tag_path, class: "btn btn-primary") %>

<%= render 'layouts/pagination' %>

<table class="table table-striped table-bordered table-hover">
  <tr>
    <th>Name</th>
    <th>Class</th>
    <th></th>
  </tr>

  <%=render @tags %>

</table>

<%= render 'layouts/pagination' %>

<% provide :leftNavButtonBody, 'Home' %>
<% provide :leftNavButtonUrl, root_path %>