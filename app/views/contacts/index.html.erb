<% provide(:title, 'Contacts') %>
<% provide(:titleSmall, 'All') %>

<% provide :actions, link_to(raw('<i class="icon-plus icon-white"></i> New Contact'), new_contact_path, class: "btn btn-small btn-primary") %>

<%= render 'layouts/pagination' %>

<table class="table table-striped table-bordered table-hover">
  <tr>
    <th>Name</th>
    <th></th>
  </tr>

  <%=render @contacts %>

</table>

<%= render 'layouts/pagination' %>

<% provide :leftNavButtonBody, 'Home' %>
<% provide :leftNavButtonUrl, root_path %>