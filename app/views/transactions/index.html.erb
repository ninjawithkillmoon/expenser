<% provide(:title, 'Transactions') %>
<% provide(:titleSmall, 'All') %>

<% provide :actions, link_to(raw('<i class="icon-plus icon-white"></i> New Transaction'), new_transaction_path, class: "btn btn-small btn-primary") %>

<%= render 'layouts/pagination' %>

<table class="table table-striped table-bordered table-hover">
  <tr>
    <th>Description</th>
    <th>Amount</th>
    <th>Tags</th>
    <th>Account</th>
    <th>Date</th>
    <th></th>
  </tr>

  <%=render @transactions %>

</table>

<%= render 'layouts/pagination' %>

<% provide :leftNavButtonBody, 'Home' %>
<% provide :leftNavButtonUrl, root_path %>